<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <div class="btn-group" role="group">
        <% if current_user  %>
          <a class="btn btn-warning bi bi-list" id="menu" data-bs-toggle="offcanvas" href="#offcanvasExample" role="button" aria-controls="offcanvasExample"></a>    
        <% end %>
      <a id="name_ucode" href="/">UChat</a>
    </div> 
    <%# <div>
      <% if user_signed_in?  %>
        <%# <h5>Hola! <%= current_user.nickname.capitalize %></h5>
      <%# <% end %>
    <%# </div> %>
    <div class="btn-group" role="group">       
      <% if current_user %>
            <h5 id="nick_perfil">Hola! <%= current_user.username.capitalize%></h5> 
            <%# <h5 id="nick_perfil1"><span class="typed"></span></h5>     %>
          <%= button_to '', destroy_user_session_path, method: :delete, form: {data: {turbo_confirm: 'Esta seguro?'}}, class: 'btn btn-light bi bi-door-closed', id:'btnLogout' %>
      <% else  %> 
        <%= link_to "", new_user_session_path, class: 'btn btn-light bi bi-person-fill', id:'btnSign_in' %> 
      <% end %>   
    </div>
  </div>
</nav>

<% if current_user %>
<div class="offcanvas offcanvas-start" tabindex="-1" id="offcanvasExample" aria-labelledby="offcanvasExampleLabel">
  <div class="offcanvas-header">
  <%# <button type="button" id="btnClose" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button> %>
    <%= image_tag('logo5', id:'logo_menu') %>
    <h3 class="offcanvas-title"><%= current_user.username.capitalize %>
  </div>   
  <div class="offcanvas-body">
    <%# <center><h3></h3></center> %>
    <hr>
    <%# <h3>Mis Publicaciones:</h3> %>
    <div id="content_Mpublics">
      <%= link_to "Mis Publicaciones", user_publish_publishes_path(current_user.id), id:"btnMispiblicaciones" %>
    </div>
    <hr>
    <div id="content_Mcomment">
        <%= link_to "Mis Comentarios", comments_path, id:"btnMiscomment" %>
    </div> 
    <hr>
    <% if current_user&.admin? %>
      <div id="content_Mcomment">
          <%= link_to "Admin", admin_dashboard_path, id:"btnMiscomment", class: "nav-link" if current_user&.admin?%>
      </div>
      <hr>
    <% end %>
    <div id="content_Mcomment">
      <%= link_to "Start Chatting", rooms_path, id:"btnMiscomment" %>
    </div>
    <hr>
  </div>
  <% end %>
</div>     

